# Copy this to .env and fill in your values.
# .env is gitignored and should never be committed.

# Docker Hub credentials
DOCKERHUB_USER=rick0x00
DOCKERHUB_TOKEN=

# Image tag (defaults to "latest" if not set)
# Override at task invocation: IMAGE_TAG=abc123 task deploy:image-build
# IMAGE_TAG=latest
