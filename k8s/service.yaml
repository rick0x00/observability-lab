apiVersion: v1
kind: Service
metadata:
  name: observability-app
  namespace: app
  labels:
    app: observability-app
    environment: staging
    component: app
spec:
  type: ClusterIP
  selector:
    app: observability-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
